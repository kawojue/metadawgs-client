import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["pbs.twimg.com", "asset.cloudinary.com", "res.cloudinary.com"],
  },
};

export default nextConfig;
